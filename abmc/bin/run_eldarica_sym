#!/bin/bash
HELP=0
if (( $# == 1 )); then
    TO=300
elif (( $# == 2 )); then
    TO=$2
else
    HELP=1
    echo "wrong number of arguments"
fi
if [ "$1" = "--help" ]; then
    HELP=1
fi
if (( $HELP == 1 )); then
    echo "usage: run_eldarica_sym INPUT [TIMEOUT]"
    echo "       The timeout is given in seconds."
    echo "       If no timeout is specified, then it defaults to 300s."
else
    timeout --foreground $TO ./eld -splitClauses:1 -sym $1
fi
